{% extends '_commun/headers/base.html.twig' %}

{% block menuItems %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('admin_session_index') }}">{{ 'session.list.title'|trans }}</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path('admin_movie_index') }}">{{ 'movie.list.title'|trans }}</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path('admin_rule_index') }}">{{ 'rule.list.title'|trans }}</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path('admin_participant_index') }}">{{ 'rule.list.title'|trans }}</a>
    </li>
{% endblock %}