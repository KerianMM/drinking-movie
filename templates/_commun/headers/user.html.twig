{% extends '_commun/headers/base.html.twig' %}

{% block menuItems %}
    <li class="nav-item active">
        <a class="nav-link" href="{{ path('session_index') }}">{{ 'session.list.title'|trans }}</a>
    </li>
{% endblock %}