{% extends 'base.html.twig' %}

{% block title %}Rule{% endblock %}

{% block body %}
    <h1>Rule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rule.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ rule.description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rule.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rule_index') }}">back to list</a>

    <a href="{{ path('rule_edit', {'id': rule.id}) }}">edit</a>

    {{ include('rule/_delete_form.html.twig') }}
{% endblock %}
